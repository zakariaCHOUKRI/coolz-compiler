class Sum {
    num1 : Int;
    num2 : Int;

    init(a : Int, b : Int) : Sum {
        {
            num1 <- a;
            num2 <- b;
            self;
        }
    };

    sum() : Int {
        num1 + num2
    };
};